@model Decimal?

@{

    Decimal dt = 0;

    if (Model != null)
    {

        dt = (System.Decimal)Model;

    }

    @Html.TextBox("", String.Format("{0:d}", dt.ToString("F1")), new { @class = "form-control decimalcontrol", type = "decimal" })

}
